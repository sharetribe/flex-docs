import React from 'react';

import { ThanksForTheFeedbackPage } from '../components';

/**
 * This is the page that the feedback form redirects to when
 * finished. The feedback form link is in the end of the landing page.
 *
 * NOTE: Do not change the page URL!
 */
export default () => {
  return <ThanksForTheFeedbackPage />;
};
